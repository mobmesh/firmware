# Mod-level facts, distinct from the per-patch sidecars (0001.meta.yaml)
# which declare per-patch env contributions. This file holds facts about
# the mod as a whole.
name: adc-accuracy
# No suffix -- this corrects a measurement bug rather than adding a feature,
# so it shouldn't change any released asset's filename.
suffix: ""
