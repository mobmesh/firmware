# Mod-level facts. build_src_filter is derived from what is under files/, so the
# two cannot drift apart; this mod ships headers only, so it contributes none.
name: power-guard

env_flags: [WITH_POWER_GUARD]
