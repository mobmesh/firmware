diff --git a/examples/simple_repeater/main.cpp b/examples/simple_repeater/main.cpp
index a714db68..8d5b8841 100644
--- a/examples/simple_repeater/main.cpp
+++ b/examples/simple_repeater/main.cpp
@@ -2,6 +2,7 @@
 #include <Mesh.h>
 
 #include "MyMesh.h"
+#include <helpers/ModHooks.h>
 
 #ifdef DISPLAY_CLASS
   #include "UITask.h"
@@ -60,7 +61,7 @@ void setup() {
   }
 #endif
 
-  if (!radio_init()) {
+  if (!modRadioInit(FIRMWARE_VERSION "-" OTA_MOD_SHORT_SHA)) {
     MESH_DEBUG_PRINTLN("Radio init failed!");
     halt();
   }
@@ -123,6 +124,7 @@ void setup() {
 }
 
 void loop() {
+  modLoop();
   // Handle Serial CLI
   int len = strlen(command);
   while (Serial.available() && len < sizeof(command)-1) {
@@ -195,7 +197,7 @@ void loop() {
 #ifdef HAS_EXTERNAL_WATCHDOG
   external_watchdog.loop();
 #endif
-  if (the_mesh.getNodePrefs()->powersaving_enabled && !the_mesh.hasPendingWork()) {
+  if ((the_mesh.getNodePrefs()->powersaving_enabled || modWantsPowerSaving()) && !the_mesh.hasPendingWork()) {
 #if defined(NRF52_PLATFORM)
     board.sleep(0); // nrf ignores seconds param, sleeps whenever possible
 #else
diff --git a/examples/simple_room_server/main.cpp b/examples/simple_room_server/main.cpp
index d833fff3..f2abfe85 100644
--- a/examples/simple_room_server/main.cpp
+++ b/examples/simple_room_server/main.cpp
@@ -2,6 +2,7 @@
 #include <Mesh.h>
 
 #include "MyMesh.h"
+#include <helpers/ModHooks.h>
 
 #ifdef ETHERNET_ENABLED
   #define ETHERNET_CLI_BANNER "MeshCore Room Server CLI"
@@ -45,7 +46,7 @@ void setup() {
   }
 #endif
 
-  if (!radio_init()) { halt(); }
+  if (!modRadioInit(FIRMWARE_VERSION "-" OTA_MOD_SHORT_SHA)) { halt(); }
 
   fast_rng.begin(radio_driver.getRngSeed());
 
@@ -104,6 +105,7 @@ void setup() {
 }
 
 void loop() {
+  modLoop();
   int len = strlen(command);
   while (Serial.available() && len < sizeof(command)-1) {
     char c = Serial.read();
diff --git a/src/helpers/CommonCLI.cpp b/src/helpers/CommonCLI.cpp
index b318bb58..7ebc2067 100644
--- a/src/helpers/CommonCLI.cpp
+++ b/src/helpers/CommonCLI.cpp
@@ -180,6 +180,7 @@ uint8_t CommonCLI::buildAdvertData(uint8_t node_type, uint8_t* app_data) {
 }
 
 void CommonCLI::handleCommand(uint32_t sender_timestamp, char* command, char* reply) {
+    if (handleModCommand(sender_timestamp, command, reply)) return;   // CommonCliMods.cpp
     if (memcmp(command, "poweroff", 8) == 0 || memcmp(command, "shutdown", 8) == 0) {
       _board->powerOff();  // doesn't return
     } else if (memcmp(command, "reboot", 6) == 0) {
@@ -447,6 +448,7 @@ void CommonCLI::handleCommand(uint32_t sender_timestamp, char* command, char* re
 
 void CommonCLI::handleSetCmd(uint32_t sender_timestamp, char* command, char* reply) {
   const char* config = &command[4];
+  if (handleModSetCmd(sender_timestamp, command, reply)) return;   // CommonCliMods.cpp
   if (memcmp(config, "dutycycle ", 10) == 0) {
     float dc = atof(&config[10]);
     if (dc < 1 || dc > 100) {
@@ -806,6 +808,7 @@ void CommonCLI::handleSetCmd(uint32_t sender_timestamp, char* command, char* rep
 
 void CommonCLI::handleGetCmd(uint32_t sender_timestamp, char* command, char* reply) {
   const char* config = &command[4];
+  if (handleModGetCmd(sender_timestamp, command, reply)) return;   // CommonCliMods.cpp
   if (memcmp(config, "dutycycle", 9) == 0) {
     float dc = 100.0f / (_prefs->airtime_factor + 1.0f);
     int dc_int = (int)dc;
diff --git a/src/helpers/CommonCLI.h b/src/helpers/CommonCLI.h
index 237c758e..8d2c0198 100644
--- a/src/helpers/CommonCLI.h
+++ b/src/helpers/CommonCLI.h
@@ -267,6 +267,11 @@ class CommonCLI {
   void handleGetCmd(uint32_t sender_timestamp, char* command, char* reply);
   void handleSetCmd(uint32_t sender_timestamp, char* command, char* reply);
 
+  // Defined in helpers/esp32/CommonCliMods.cpp -- return true if the mod took the command.
+  bool handleModCommand(uint32_t sender_timestamp, char* command, char* reply);
+  bool handleModSetCmd(uint32_t sender_timestamp, char* command, char* reply);
+  bool handleModGetCmd(uint32_t sender_timestamp, char* command, char* reply);
+
 public:
   CommonCLI(mesh::MainBoard& board, mesh::RTCClock& rtc, SensorManager& sensors, RegionMap& region_map, ClientACL& acl, NodePrefs* prefs, CommonCLICallbacks* callbacks)
       : _board(&board), _rtc(&rtc), _sensors(&sensors), _region_map(&region_map), _acl(&acl), _prefs(prefs), _callbacks(callbacks) { }
