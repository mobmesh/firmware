diff --git a/docs/cli_commands.md b/docs/cli_commands.md
index 2653fd4..bd32316 100644
--- a/docs/cli_commands.md
+++ b/docs/cli_commands.md
@@ -153,6 +153,24 @@ budget (~115s). `get ota.wan` checks WAN reachability on demand, repeatable with
 
 ---
 
+#### View the post-update rollback confirmation status
+**Usage:**
+- `get ota.rollback`
+
+**Returns:**
+- `pending`: This boot is on probation after an OTA update (via either `start ota` or `start ota url`).
+  The device will automatically confirm itself as valid ~90 seconds after boot if the radio initializes
+  correctly; an unconfirmed reset before then reverts to the previous firmware automatically.
+- `valid`: Already confirmed, or this boot isn't the result of an OTA update in the first place.
+- `n/a`: Rollback state could not be queried for the running partition.
+
+**Note:** Read-only — no manual override. Confirms automatically after the confirm delay with a
+working radio; rejects immediately (rollback + reboot) if `radio_init()` fails on a probationary boot.
+
+**Requires:** `WITH_OTA_ROLLBACK_GUARD` build flag (Heltec V4 repeater/room server only)
+
+---
+
 ### Erase/Factory Reset
 **Usage:**
 - `erase`
diff --git a/examples/simple_repeater/main.cpp b/examples/simple_repeater/main.cpp
index 2ce056f..262dab9 100644
--- a/examples/simple_repeater/main.cpp
+++ b/examples/simple_repeater/main.cpp
@@ -3,6 +3,10 @@
 
 #include "MyMesh.h"
 
+#if defined(ESP32) && defined(WITH_OTA_ROLLBACK_GUARD)
+#include <helpers/esp32/RollbackGuard.h>
+#endif
+
 #ifdef DISPLAY_CLASS
   #include "UITask.h"
   static UITask ui_task(display);
@@ -48,10 +52,27 @@ void setup() {
   }
 #endif
 
+#if defined(ESP32) && defined(WITH_OTA_ROLLBACK_GUARD)
+  SPIFFS.begin(true);   // RollbackGuard's failure counter needs it mounted before use; safe to call twice
+
+  // A few immediate retries catch a transient glitch before falling back to onRadioInitFailure()'s
+  // cross-boot retry-with-cap.
+  bool radio_ok = false;
+  for (int attempt = 0; attempt < 3; attempt++) {
+    if (radio_init()) { radio_ok = true; break; }
+    delay(500);
+  }
+  if (!radio_ok) {
+    MESH_DEBUG_PRINTLN("Radio init failed!");
+    RollbackGuard::onRadioInitFailure();   // does not return
+  }
+  RollbackGuard::begin();
+#else
   if (!radio_init()) {
     MESH_DEBUG_PRINTLN("Radio init failed!");
     halt();
   }
+#endif
 
   fast_rng.begin(radio_driver.getRngSeed());
 
@@ -152,6 +173,10 @@ void loop() {
 #endif
   rtc_clock.tick();
 
+#if defined(ESP32) && defined(WITH_OTA_ROLLBACK_GUARD)
+  RollbackGuard::poll();
+#endif
+
   if (the_mesh.getNodePrefs()->powersaving_enabled && !the_mesh.hasPendingWork()) {
 #if defined(NRF52_PLATFORM)
     board.sleep(0); // nrf ignores seconds param, sleeps whenever possible
diff --git a/examples/simple_room_server/main.cpp b/examples/simple_room_server/main.cpp
index a3798b2..ba0420d 100644
--- a/examples/simple_room_server/main.cpp
+++ b/examples/simple_room_server/main.cpp
@@ -3,6 +3,10 @@
 
 #include "MyMesh.h"
 
+#if defined(ESP32) && defined(WITH_OTA_ROLLBACK_GUARD)
+#include <helpers/esp32/RollbackGuard.h>
+#endif
+
 #ifdef DISPLAY_CLASS
   #include "UITask.h"
   static UITask ui_task(display);
@@ -33,7 +37,23 @@ void setup() {
   }
 #endif
 
+#if defined(ESP32) && defined(WITH_OTA_ROLLBACK_GUARD)
+  SPIFFS.begin(true);   // RollbackGuard's failure counter needs it mounted before use; safe to call twice
+
+  // A few immediate retries catch a transient glitch before falling back to onRadioInitFailure()'s
+  // cross-boot retry-with-cap.
+  bool radio_ok = false;
+  for (int attempt = 0; attempt < 3; attempt++) {
+    if (radio_init()) { radio_ok = true; break; }
+    delay(500);
+  }
+  if (!radio_ok) {
+    RollbackGuard::onRadioInitFailure();   // does not return
+  }
+  RollbackGuard::begin();
+#else
   if (!radio_init()) { halt(); }
+#endif
 
   fast_rng.begin(radio_driver.getRngSeed());
 
@@ -115,4 +135,8 @@ void loop() {
   ui_task.loop();
 #endif
   rtc_clock.tick();
+
+#if defined(ESP32) && defined(WITH_OTA_ROLLBACK_GUARD)
+  RollbackGuard::poll();
+#endif
 }
diff --git a/src/helpers/CommonCLI.cpp b/src/helpers/CommonCLI.cpp
index 5114149..e503e85 100644
--- a/src/helpers/CommonCLI.cpp
+++ b/src/helpers/CommonCLI.cpp
@@ -15,6 +15,10 @@
 #define OTA_MOD_BUILD_DATE "unknown"
 #endif
 
+#if defined(ESP_PLATFORM) && defined(WITH_OTA_ROLLBACK_GUARD)
+#include <helpers/esp32/RollbackGuard.h>
+#endif
+
 #ifndef BRIDGE_MAX_BAUD
 #define BRIDGE_MAX_BAUD 115200
 #endif
@@ -994,6 +998,10 @@ void CommonCLI::handleGetCmd(uint32_t sender_timestamp, char* command, char* rep
   } else if (memcmp(config, "ota.wan", 7) == 0) {
     // Pre-flight check -- see `start ota wifi.join`. Assumes WiFi already joined.
     HotspotOTA::checkWan(reply);
+#endif
+#if defined(ESP_PLATFORM) && defined(WITH_OTA_ROLLBACK_GUARD)
+  } else if (memcmp(config, "ota.rollback", 12) == 0) {
+    sprintf(reply, "> %s", RollbackGuard::status());
 #endif
   } else {
     sprintf(reply, "??: %s", config);
diff --git a/src/helpers/esp32/RollbackGuard.cpp b/src/helpers/esp32/RollbackGuard.cpp
new file mode 100644
index 0000000..e879127
--- /dev/null
+++ b/src/helpers/esp32/RollbackGuard.cpp
@@ -0,0 +1,88 @@
+#include "RollbackGuard.h"
+#include <esp_ota_ops.h>
+#include <esp_system.h>   // esp_restart()
+#include <SPIFFS.h>
+
+// Long enough to catch an early crash/boot-loop; short enough not to add much to the ~2 minutes a
+// hotspot-fetch update can already take.
+#define OTA_ROLLBACK_CONFIRM_DELAY_MS   90000
+
+// Cross-boot retry cap for onRadioInitFailure() -- gives a transient radio failure a few boot
+// attempts to clear before bottoming out at a permanent halt. SPIFFS used for the counter instead
+// of RTC memory (unreliable across esp_restart() on this chip/IDF combo).
+#define RADIO_INIT_RESET_CAP   5
+#define RADIO_FAIL_COUNT_PATH  "/radio_fail_count"
+
+// Arduino's own rollback-confirmation hook runs inside initArduino(), before setup() -- too early to
+// judge anything. This overrides it to defer to RollbackGuard instead. Must be extern "C": the weak
+// symbol it replaces is declared in a plain .c file, and C++ mangling would otherwise link a
+// different, unused symbol.
+extern "C" bool verifyRollbackLater() { return true; }
+
+static uint32_t boot_time_ms = 0;
+static bool confirmed_or_not_applicable = false;
+
+static bool isPendingVerify(esp_ota_img_states_t* out_state = nullptr) {
+  esp_ota_img_states_t state = ESP_OTA_IMG_UNDEFINED;
+  const esp_partition_t* running = esp_ota_get_running_partition();
+  esp_ota_get_state_partition(running, &state);
+  if (out_state) *out_state = state;
+  return state == ESP_OTA_IMG_PENDING_VERIFY;
+}
+
+static uint8_t readFailCount() {
+  File f = SPIFFS.open(RADIO_FAIL_COUNT_PATH, "r");
+  if (!f) return 0;
+  uint8_t count = 0;
+  f.read(&count, 1);
+  f.close();
+  return count;
+}
+
+static void writeFailCount(uint8_t count) {
+  File f = SPIFFS.open(RADIO_FAIL_COUNT_PATH, "w");
+  if (!f) return;
+  f.write(&count, 1);
+  f.close();
+}
+
+void RollbackGuard::begin() {
+  boot_time_ms = millis();
+  confirmed_or_not_applicable = !isPendingVerify();
+  writeFailCount(0);   // successful boot -- clear any prior failure streak
+}
+
+void RollbackGuard::poll() {
+  if (confirmed_or_not_applicable) return;
+  if (millis() - boot_time_ms < OTA_ROLLBACK_CONFIRM_DELAY_MS) return;
+  esp_ota_mark_app_valid_cancel_rollback();
+  confirmed_or_not_applicable = true;
+}
+
+bool RollbackGuard::reportUnhealthy() {
+  if (!isPendingVerify()) return false;
+  return esp_ota_mark_app_invalid_rollback_and_reboot() == ESP_OK;   // doesn't return on success
+}
+
+void RollbackGuard::onRadioInitFailure() {
+  reportUnhealthy();   // OTA-probation case: forces rollback + reboot, doesn't return on success
+
+  // Not on probation -- transient or genuine radio failure unrelated to any update. Retry with cap
+  // instead of hanging forever on the first attempt.
+  uint8_t count = readFailCount();
+  if (count < RADIO_INIT_RESET_CAP) {
+    writeFailCount(count + 1);
+    esp_restart();   // does not return
+  }
+
+  // Cap exhausted -- same terminal behavior as before this feature existed.
+  while (1) ;
+}
+
+const char* RollbackGuard::status() {
+  esp_ota_img_states_t state;
+  if (!isPendingVerify(&state)) {
+    return (state == ESP_OTA_IMG_VALID) ? "valid" : "n/a";
+  }
+  return "pending";
+}
diff --git a/src/helpers/esp32/RollbackGuard.h b/src/helpers/esp32/RollbackGuard.h
new file mode 100644
index 0000000..2693188
--- /dev/null
+++ b/src/helpers/esp32/RollbackGuard.h
@@ -0,0 +1,32 @@
+#pragma once
+#include <Arduino.h>
+
+// Confirms or rejects the running firmware during ESP-IDF's post-OTA "pending verify" window. A
+// no-op on any normal, already-confirmed boot -- only acts on the first boot after an OTA update.
+//
+// Arduino's initArduino() auto-confirms every new image via a weak `verifyOta()` hook (default
+// `return true`) before setup() runs -- RollbackGuard.cpp overrides it to defer that decision here.
+namespace RollbackGuard {
+  // Call once from setup(), after radio_init() succeeds. Records the boot time for poll() and
+  // clears the persisted radio-failure counter so an unrelated future failure counts fresh.
+  void begin();
+
+  // Call every loop() iteration. Marks the image valid once the confirm delay has elapsed since
+  // begin(). No-op before that and a cheap no-op forever after confirmation.
+  void poll();
+
+  // Call from a failure path to report the firmware unhealthy. If this boot is pending-verify,
+  // forces immediate rollback + reboot (does not return). Otherwise returns false so the caller
+  // falls back to its own handling (nothing to roll back to, or not on probation).
+  bool reportUnhealthy();
+
+  // Call from radio_init() failure after in-boot retries are exhausted. Tries reportUnhealthy()
+  // first; if not applicable, falls back to a persisted cross-boot retry-with-cap, then halts
+  // permanently once exhausted -- same terminal behavior as before this feature existed. Does not
+  // return.
+  void onRadioInitFailure();
+
+  // For `get ota.rollback` -- "pending", "valid" (confirmed or n/a to this boot), or "n/a" (feature
+  // unavailable for this partition).
+  const char* status();
+}
diff --git a/variants/heltec_v4/platformio.ini b/variants/heltec_v4/platformio.ini
index c0f750e..70cf945 100644
--- a/variants/heltec_v4/platformio.ini
+++ b/variants/heltec_v4/platformio.ini
@@ -93,10 +93,12 @@ build_flags =
   -D ADMIN_PASSWORD='"password"'
   -D MAX_NEIGHBOURS=50
   -D WITH_HOTSPOT_OTA=1
+  -D WITH_OTA_ROLLBACK_GUARD=1
 ;  -D MESH_PACKET_LOGGING=1
 ;  -D MESH_DEBUG=1
 build_src_filter = ${heltec_v4_oled.build_src_filter}
   +<helpers/esp32/HotspotOTA.cpp>
+  +<helpers/esp32/RollbackGuard.cpp>
   +<helpers/ui/SSD1306Display.cpp>
   +<../examples/simple_repeater>
 lib_deps =
@@ -159,10 +161,12 @@ build_flags =
   -D ADMIN_PASSWORD='"password"'
   -D ROOM_PASSWORD='"hello"'
   -D WITH_HOTSPOT_OTA=1
+  -D WITH_OTA_ROLLBACK_GUARD=1
 ;  -D MESH_PACKET_LOGGING=1
 ;  -D MESH_DEBUG=1
 build_src_filter = ${heltec_v4_oled.build_src_filter}
   +<helpers/esp32/HotspotOTA.cpp>
+  +<helpers/esp32/RollbackGuard.cpp>
   +<helpers/ui/SSD1306Display.cpp>
   +<../examples/simple_room_server>
 lib_deps =
